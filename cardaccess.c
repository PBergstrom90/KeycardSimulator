#include <stdio.h>
#include "cardaccess.h"
#include "card.h"
#include "cardlist.h"

void cardAccess(cardList) {

    printf("This is a test!\n");
    // for (int i = 0; i < count; i++){
    //     printf("%d. %s\n", i+1, list[i].cardId);
    // }
    // int cardSelect;
    // GetInputInt("Enter the card ID-number: ", &cardSelect);
    // printf("You want to change the access for: %s to: ", list[cardSelect-1].cardId);
    // GetInput("New card access: ", list[cardSelect-1].cardId,20);
}